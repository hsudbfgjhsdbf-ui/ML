# Results discussion

The selected deep model is **Feature-token transformer**. See `evaluation2/leaderboard.csv` for every architecture. The conclusion is bounded by the supplied 4,500-row snapshot, missing policy/document evidence, and possible synthetic shortcuts.
